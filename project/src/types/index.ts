export type Email = {
  email: string,
};

export type ID = {
  _id: string,
};

export type Login = {
  email: string,
  password: string,
};

export type TokenType = {
  id: string;
  email: string;
};